apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sm
  labels:
    team: sm-team
spec:
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      app.kubernetes.io/name: traefik
  endpoints:
  - port: metrics